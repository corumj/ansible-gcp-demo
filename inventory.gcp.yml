plugin: gcp_compute
projects:
  - terraria-usp
keyed_groups:
  - key: label
groups:
  terraria: "'terraria' in name"
auth_kind: serviceaccount
service_account_file: "{{ lookup('env', 'GCP_SERVICE_ACCT_FILE') }}"
